export const publicRoutes = ['/', '/database', '/tierlist', '/builds']

export const authRoutes = []

export const apiAuthPrefix = '/api/auth'

export const DEFAULT_LOGIN_REDIRECT = '/my-builds'

export const protectedRoutes = ['/my-builds', '/build-planner']
